#ifndef ELEMENTS_H
#define ELEMENTS_H

#include <genesis.h>

// retângulo em tiles (8x8 px) usado pelos elementos que compõem uma fase
typedef struct { u16 x, y, w, h; } TileRect;

#endif // ELEMENTS_H
